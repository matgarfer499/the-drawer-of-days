# Context7 MCP — up-to-date library docs for the agent.
# Get a key at https://context7.com (Dashboard → API key), then copy this file to
# `.env.local` (gitignored) or export it in your shell. NEVER commit the real value.
CONTEXT7_API_KEY=ctx7sk-xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx
