## What & why

<!-- What does this change, and why? Link the ROADMAP phase if relevant. -->

## Checklist

- [ ] `pnpm check` and `pnpm typecheck` pass
- [ ] `pnpm test` passes
- [ ] Respects the experience invariants (no page scroll · invariable close gesture · `prefers-reduced-motion` fallback · content via Zod · audio singleton)
- [ ] Verified on a mobile viewport
